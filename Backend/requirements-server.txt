# =============================================================================
# Dismissal Backend (FastAPI on Cloud Run)
# =============================================================================
# Lean dependency list for the API server only.
# ML/scanner packages (tensorflow, torch, opencv) live in requirements.txt
# and are NOT needed by the Cloud Run container.
# =============================================================================

fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.6
firebase-admin==6.7.0
# Required directly by server.py (`from google.cloud import firestore`).
# Without this pin we silently rely on whatever google-cloud-firestore
# firebase-admin happens to bring in transitively, which has bitten us
# before — most recently when an older revision shipped without the
# /api/v1/admin/guardians route, leaving the admin Guardians page 404ing
# in production while the route was sitting in master the whole time.
google-cloud-firestore==2.20.1
cryptography==44.0.2
python-dotenv==1.1.0
PyJWT==2.10.1
requests==2.32.3
