# =============================================================================
# Dismissal Scanner — Raspberry Pi + Google Coral TPU
# =============================================================================
# Install on the RPi with:
#   pip install -r requirements-scanner.txt --break-system-packages
#
# Tesseract system package must also be installed:
#   sudo apt-get install -y tesseract-ocr tesseract-ocr-eng
#
# For Coral TPU support install pycoral AFTER this file:
#   See https://coral.ai/docs/accelerator/get-started/
#   pip install pycoral tflite-runtime
# =============================================================================

# --- Core scanner deps ---
opencv-python-headless==4.11.0.86   # headless build — no GUI needed on RPi
pytesseract==0.3.13                 # Python wrapper for Tesseract OCR
Pillow==11.1.0                      # image format support for pytesseract
numpy==2.1.3
requests==2.32.3
python-dotenv==1.1.0

# --- Coral TPU (optional — install separately, see above) ---
# pycoral
# tflite-runtime

# --- NOT included (backend-only, do not install on RPi) ---
# fastapi, uvicorn, firebase-admin, google-cloud-firestore, tensorflow, torch
